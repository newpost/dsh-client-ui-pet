# AGENTS.md

Guides for AI agents working in this repo. Edit `docs/agents/*.md` to change the underlying configuration; re-run `/setup-matt-pocock-skills` only to switch issue trackers or restart from scratch.

## Agent skills

### Issue tracker

Issues for this repo live in GitHub Issues; use the `gh` CLI for all operations. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one root `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
